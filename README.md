# playground-websites

A sandbox for building small websites with plain **HTML**, **CSS**, and **JavaScript**.

## Quick start

Open `index.html` in your browser, or serve the folder:

```bash
# Python
python -m http.server 8080

# Node (if you have npx)
npx --yes serve .
```

Then visit http://localhost:8080

## Layout

| File | Role |
|------|------|
| `index.html` | Page structure |
| `styles.css` | Styling |
| `script.js` | Interactivity |

## Ideas for next experiments

- A personal landing page
- A recipe card or link-in-bio page
- A tiny to-do list
- CSS-only layout challenges (grid/flex)

Happy building.
